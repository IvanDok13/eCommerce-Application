export interface ProductCardProps {
  id: string;
  name: string;
  price: number;
  artist: string;
  color: string;
  size: string;
  imageUrls: string[];
  // cartItems: { productId: string }[];
}
